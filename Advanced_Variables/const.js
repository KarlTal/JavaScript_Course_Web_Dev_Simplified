//Define variables with const instead of let
//Cannot change the value after definition

//Main reason is to define a variable that will never change
//Use const by default and only use let if value needs to be changed
let a = 1;
a = 2
console.log(a);

const b = 2
//b = 3
console.log(b)


