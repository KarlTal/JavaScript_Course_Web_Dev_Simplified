let a = "Hello";
let b = "World";

// ` => Use case combine with variables to make strings nice => known as template literals or template string

console.log(`${a} ${b}`);

//Create 2 variables, one for first name, one for last name and combine

let firstName = "Karl";
let lastName = "Talavera";
console.log(`${firstName} ${lastName}`);
